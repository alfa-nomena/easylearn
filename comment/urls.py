from .import views
from django.urls import path


app_name="comment"
urlpatterns = [
    # path("create", views.OwnerCreateView.as_view(), name="create"),
    # path("get/all", views.OwnerListView.as_view(), name="get-all"),
    # path("get/one/<str:public_id>", views.OwnerDetailView.as_view(), name="get-one"),
    # path("update/<str:public_id>", views.OwnerUpdateView.as_view(), name="create"),
    # path("delete/<str:public_id>", views.OwnerDeleteView.as_view(), name="delete"),
]
